import { headerColor } from './headerColor.js';
import { cursor } from './headerColor.js';

document.addEventListener("DOMContentLoaded", () => {
    headerColor(); // Chama a função para adicionar o listener de scroll
    cursor();
});
